# 低温水獭内容项目规则

## 项目目标

为抖音账号“低温水獭”持续生产温和、清醒、克制、有内容密度的访谈类图文。优先保证观点完整、语序连贯和视觉一致，不以机械日更牺牲质量。

## 每次开始前

1. 先读取 `data/选题台账.csv`、`data/来源台账.csv` 和 `data/发布记录.csv`。
2. 查重维度包括人物、来源视频、核心观点、标题承诺和关键词；近 60 天不重复同一核心论点。
3. 确定选题后，先在选题台账新增或更新记录，将状态设为“制作中”，再开始写作和制图。
4. 如果台账与用户最新指令冲突，以用户最新指令为准，并同步更新台账。

## 账号内容范围

- 女性成长、女性力量、自我认同、边界感。
- 搞钱认知、职业成长、人际关系、长期主义。
- 自律、行动、励志、显化的理性表达。
- AI 对生活、工作、判断力和个人能力的影响。
- AI 内容以认知变化为主，不把账号做成工具教程号。

## 选句标准

- 一个作品必须把一件事或一个道理讲清楚。
- 默认 8 页，最低 3 页；可以多于 8 页，但不能为凑页数重复表达。
- 每页最多 5 句话，每页只承担一个小观点。
- 页面之间应形成“提出问题—解释原因—举例或转折—给出新理解—收束”的递进。
- 不采用空泛、断裂、只适合单句传播但不能支撑完整观点的金句。
- 翻译必须核对人名、数字、事实和专有名词；允许为中文阅读节奏润色，但不得改变原意。
- 字幕统一采用“来源锁定的中文凝练”：每条中文必须能由原视频的一句或相邻连续语义直接支持。
- 允许删除口头语、合并相邻表达、拆句和调整中文语序；禁止增加原视频没有的新观点、新因果、联想或编辑总结。
- 脚本字幕模式必须为每条中文字幕保存 `source_time` 和简短的 `original_excerpt`，以便逐句复核。
- 页面小标题可以概括本页原意，但同样不得扩大原视频结论；发布标题和正文可做编辑表达，但不能冒充讲者原话。

## 画面获取

- 默认使用 Edge 中已安装的开源 `YouTube Screenshot` 扩展获取画面。
- 在 YouTube 中先切换到最高可用画质，等待画面缓冲，再导出 PNG 视频帧。
- 一页只使用一张人物截图；8 页作品通常只需要 8 张状态各异的画面。
- 画面时间只负责视觉表现，字幕来源时间负责内容追溯；不要求人物表情逐句对应。
- 优先选择正面、侧面、微笑、思考、手势、近景、半身和收束画面，避免 8 页高度重复。
- 不截浏览器按钮、播放器进度条、通知、转场、黑帧、闭眼或尴尬口型。
- 默认不下载完整视频。只有截图扩展失效、需要原生烧录字幕或确有复核需要时，才下载 1080P 局部片段；完整 4K 视频不得作为默认方案。

## 默认视觉版式

- 严格遵循 `templates/低温水獭_黑金访谈版式.md`。
- 画布 1440×1920，3:4，人物截图满版铺设并适度暗化。
- 左上角必须包含两位数页码 `01 / 08` 和本页小标题。
- 右上角必须放置同尺寸的低温水獭品牌 Logo。
- 黑、白、金为主色；金色用于页码、小标题、Logo和短分隔线。
- 所有正文字幕使用同一字体、同一字号。默认微软雅黑粗体 56 px，禁止根据句长自动缩小。
- 句子过长时优先精简或拆句，不通过缩小字号解决。
- 字幕采用白字、黑色描边、窄幅半透明黑底；条与条之间不留白缝。

## 文案风格

- 语气温和但有判断，像一个懂工具、懂成长、不过度说教的朋友。
- 理性、简洁、克制；不故作高深，不用夸张营销语言。
- 标题指出真实矛盾或认知反差，不做与正文无关的标题党。
- 正文结构优先：共鸣场景—核心洞察—进一步解释—温和收束—一个可回答的问题。
- 适量使用表情，但不在严肃观点中密集堆叠。
- 禁止使用“全网最低、绝对、一定、彻底改变、看完就懂、秒变、永远不会”等无依据承诺。

## 发布包

每个作品至少交付：

- 按顺序编号的成品图片。
- 页面标题、字幕文本、画面时间和内容来源时间 JSON。
- 发布标题、正文、话题和建议首评。
- 来源人物、视频标题、URL和权限说明。
- 总览图和逐张质检结果。

完成后更新 `data/来源台账.csv`、`data/选题台账.csv` 和 `data/发布记录.csv`。发布状态不确定时写“待确认”，不得擅自标记为“已发布”。

## 在线协作

- 开始工作前先同步远程仓库；完成台账更新后尽快提交并推送。
- 两台设备同时工作时，先以“制作中”记录占用选题，避免重复生产。
- 不向 GitHub 提交完整视频、下载缓存、临时帧和未完成下载。
- 不覆盖用户已有成品；新版使用新的版本目录或清晰版本号。
